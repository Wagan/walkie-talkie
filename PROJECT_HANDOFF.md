# PROJECT_HANDOFF — walkie-talkie

Документ для безопасного возобновления работы в НОВОЙ сессии/чате (и при смене рабочего
чата). Дата: **2026-08-20**. Ветка `main`, последний коммит **`e45e9d1`** (headroom: дефолт 6 дБ
подтверждён на железе + оговорка про измеритель), удалённый `git@github.com:Wagan/walkie-talkie.git`.

> **АКТУАЛЬНО (Codec2 ИНТЕГРИРОВАН и ПРОВЕРЕН на железе — речь идёт).** Вокодер Codec2 подключён
> четвёртым кодеком лесенки LAB07 (`docs/REPORT_codec2_integration.md`): `codec codec2`,
> `c2mode 3200|2400|1600|1300|700C` (старт 3200). Кадрирование **кадр-в-кадр** (один вокодерный
> кадр = один SLIP-кадр; заголовок несёт 5-й байт = индекс режима). Кодирование/декодирование —
> **в главном цикле** (не в ISR): TX аудио-ISR децимирует 16→8 и кладёт отсчёты в SPSC-кольцо,
> `c2_tx_process` кодирует; RX декодирует в `voice_sink`/rx_drain. Один экземпляр `c2inst` (enc+dec,
> полудуплекс); смена режима destroy→create — пул не течёт. Живой замер enc/dec печатает `voice`.
> Правки только в `App/Labs/speech.c` (codec.c/.ioc/HAL/ThirdParty не тронуты). Сборка всех шести
> LAB00/02/03/04/05/07 — 0/0. **Читать счётчики по-новому:** `sent`/`recv` = вокодерные кадры (20/40 мс,
> не 5 мс), `loop_max` вырастает (железо: 3200 приём ~11 мс/передача ~34 мс; 700C передача ~52 мс) —
> норма. Живая речь дороже синтетики ~10 % (3200 = 65 % кадра, 700C = 84 %); prefill/очередь НЕ меняли
> (хватает). Качество: 3200 лучший, 1300 металлический, 700C + шумы. **Счётчики enc/dec avg** после
> железа исправлены: суммы 64-бит (32-бит переполнялся → avg мусор), сброс при смене `c2mode`; добавлен
> печатаемый `ring_over` (переполнение TX-кольца ISR→главный цикл). История —
> `REPORT_codec2_{recon,port_and_load,stack_and_speed,codebook_verify,integration}.md`. **На будущее
> (радиоэтап):** `loop_max` передачи 52 мс на 700C при кольце ~128 мс — при переходе на радио запас
> пересчитать. Тактовую 96 МГц не трогать (100 ломает USB).

---

## 1. Что за проект

Учебный курс: два кита **STM32F411E-DISCO** превращаются в пару речевых радиостанций.
Этапы: провод → речь по проводу → сменные радиоканалы (до UWB DWM3000). Владелец —
**Vagan/Wagan Sarukhanov** (прошивает и запускает железо, работает с оборудованием).

Рабочий процесс **Wagan's Team**: задачи ставятся файлами `docs/TASK_*.md`; по ним пишется код и
отчёты `docs/REPORT_*.md`, проект **собирается локально до чистой сборки** и коммитится; прошивка
на железо и работа с оборудованием — за владельцем.

## 2. Окружение (факты)

- STM32CubeIDE **1.16.0** (`C:\ST\STM32CubeIDE_1.16.0`); workspace
  `C:\Users\user\STM32CubeIDE\workspace_1.16.0`; проект в подпапке `walkie-talkie`.
- FW-пакет **STM32Cube_FW_F4_V1.28.0** распакован в
  `C:\Users\user\STM32Cube\Repository\STM32Cube_FW_F4_V1.28.0`.
- Тулчейн: arm-none-eabi-gcc **12.3.1** (GNU Tools for STM32 12.3.rel1), из плагинов CubeIDE.
- ОС Windows, оболочка PowerShell 5.1 (`&&` не поддерживается; дописывать через
  `Add-Content`, не `>>`). Bash-инструмент тоже доступен.
- Плата: PLLI2S для аудио — единый **86 МГц (M=8,N=258,R=3)**; HSE переведён владельцем в
  **кварцевый режим** (`RCC_HSE_ON`), SYSCLK/HCLK 96 МГц, APB2 96 МГц, APB1 24 МГц.

## 3. Архитектура: один проект, лабы через конфигурацию сборки

- Курс — ОДИН проект CubeIDE. Конкретная работа выбирается **конфигурацией сборки** через
  символ препроцессора **`LAB_ID`** (напр. конфигурация `LAB00` → `LAB_ID=0`,
  `LAB02` → `LAB_ID=2`). У каждой свой каталог сборки (`LAB00/`, `LAB02/` — в `.gitignore`).
- **`App/Common/`** — общий код всех работ:
  - `Inc/lab.h` — единый интерфейс работы: `uint8_t Lab_Init(void)` + `void Lab_Process(void)`;
    ТУТ же защита по `LAB_ID` (`#error` при неверной/неподдержанной конфигурации; список
    поддержанных LAB_ID держится здесь — при добавлении работы дополнять условие).
  - `Inc/trace_swo.h`, `Src/trace_swo.c` — вывод в SWO/ITM + `__io_putchar` (printf→SWO).
  - `Inc/trace_log.h` — `TRACE_LOG`/`TRACE_ERR` (уровни [I]/[E] поверх printf).
  - `Src/bsp_audio_clock.c` — сильные перекрытия `BSP_AUDIO_IN/OUT_ClockConfig` (единый PLLI2S).
- **`App/Labs/`** — по файлу на работу, весь файл обёрнут `#if LAB_ID == <n>`, реализует
  `Lab_Init`/`Lab_Process`:
  - `stand_check.c` — LAB00 (проверка стенда: микрофон→кодек, 16 кГц, + heartbeat/статус SWO).
  - `uart_link.c` — LAB02 (провод по **USART2**, пакеты, статистика, нагрузка).
  - `framing.c` — LAB03 (кадрирование SLIP + CRC-16, синхронизация, потери).
  - `voice_wire.c` — LAB04 (речь по проводу: PCM, PTT на PA0, полудуплекс, джиттер-буфер).
  - `usb_console.c` — LAB05 (USB Device CDC, консольное меню; команды regs/pintest).
  - `speech.c` — LAB07 (сжатие речи: raw/µ-law/ADPCM, 8/16 кГц, антиалиас FIR, budget/load).
- Общий транспорт вынесен в **`App/Common/uart_port.{h,c}`** (USART2, кольцевой DMA RX +
  IDLE, кадрирование), консоль — **`App/Common/console.{h,c}`**, кодеки — `codec.{h,c}`,
  аудио — `audio.{h,c}`, кадры SLIP — `frame.{h,c}`. Работы-адаптеры тонкие поверх них.
- `Core/Src/main.c` (в блоках USER CODE) вызывает только `Lab_Init()`/`Lab_Process()` —
  при добавлении работы НЕ меняется.

## 4. Жёсткие правила (соблюдать во всех задачах)

1. **Не гадать** — API HAL, поля структур, пути, номера пинов брать фактически из
   исходников/документов, не по памяти.
2. **Не редактировать** `walkie-talkie.ioc`, файлы HAL, CMSIS, `Drivers/BSP`, `ThirdParty`.
   (Периферию включает владелец в CubeMX; Wagan's Team — только прикладной код.)
3. В генерируемых файлах (`main.c`, `stm32f4xx_it.c`) правки **только внутри USER CODE**.
4. **Перед каждым коммитом собирать проект локально до ЧИСТОЙ сборки** (0 ошибок и 0
   предупреждений; warnings от своих правок устранять). Прошивка на железо и работа с
   оборудованием — за владельцем. (Раньше здесь стояло «не собирать» —
   ошибочный перенос правила про деплой; изменено владельцем 2026-08-14.) Сборка — headless
   CubeIDE, см. память `build-before-commit`.
5. Поведение уже готовых работ (LAB00) не менять.
6. **Копирайт** во всех создаваемых нами файлах:
   `Copyright (c) 1991-2026 NCPR LLC (Flexlab). All rights reserved.`, `@author Wagan Sarukhanov`.
7. **Вывод в консоль (printf/TRACE_LOG/#error) — только ASCII/английский** (консоль SWV читает
   в системной кодировке Windows, кириллица = кракозябры). Комментарии в коде — по-русски, можно.
8. STOP и вопрос владельцу при неоднозначности/расхождении с фактами.

## 5. Договорённости по процессу (стиль работы)

- Длинные отчёты (>30–40 строк) — в файл **`docs/REPORT_<задача>.md`**, на экран краткая
  сводка с именем файла. Некоторые задачи просят отчёт прямо в чат — тогда без файла.
- В конце каждой задачи: **commit + push**, хеш вписать в отчёт (обычно вторым коммитом),
  затем звуковой сигнал `[Console]::Beep(800,200);[Console]::Beep(600,200)` (PowerShell).
- `.cproject` перед правкой копируется в `.cproject.bak.before-<...>` и вносится в `.gitignore`.
- **Конфигурации LAB00/LAB02 добавлены правкой `.cproject` ВРУЧНУЮ** (копия блока Debug с
  регенерацией id, `LAB_ID`, свой `buildPath`). XML валиден, но стоит проверить, что CubeIDE
  их подхватывает; запасной путь — создать через IDE (Build Configurations → New → copy of
  Debug/LAB00, добавить `LAB_ID=N`).
- Регенерация из `.ioc` владельцем оставляет непрокоммиченные изменения (main.c, hal_conf,
  hal_msp, новые HAL-исходники и т.п.) — их включаем в коммит как «базис владельца»,
  прозрачно отмечая в отчёте, что это не наши правки.
- `walkie-talkie.launch` НЕ коммитим (в нём машинно-специфичный абсолютный путь
  `stlink.log_file`); он в `.gitignore`. Вопрос о его переносимом варианте оставлен владельцу.
- `.gitignore` игнорирует: `Debug/`, `Release/`, `/LAB[0-9][0-9]/`, `.settings/`, `.metadata/`,
  `docs/reference/`, `*.bak.before-*`, `walkie-talkie.launch`.

## 6. Состав курса и статус

Готовы и **проверены на железе**: LAB00, LAB02, LAB03, LAB04, LAB05, LAB07.

| Работа | LAB_ID | Тема | Статус |
|---|---|---|---|
| LAB00 | 0 | Проверка стенда (микрофон→кодек, SWO) | ✅ железо `App/Labs/stand_check.c` |
| LAB01 | — | Проект с нуля в CubeMX (вне репо) | 🕒 |
| LAB02 | 2 | Провод между китами, байтовый обмен по UART | ✅ железо `App/Labs/uart_link.c` |
| LAB03 | 3 | Протокол: кадрирование, синхронизация, потери | ✅ железо `App/Labs/framing.c` |
| LAB04 | 4 | Речь по проводу: PCM, полудуплекс/PTT, джиттер-буфер | ✅ железо `App/Labs/voice_wire.c` |
| LAB05 | 5 | USB Device CDC, консольное меню | ✅ железо `App/Labs/usb_console.c` |
| LAB06 | 6 | Расчёт границы канала (без железа) | ✅ реализован в `budget` (LAB07) |
| LAB07 | 7 | Сжатие речи (raw/µ-law/ADPCM + вокодер Codec2, 8/16 кГц) | ✅ железо, вкл. Codec2 (речь идёт, 3200/2400/1600/1300/700C) — `App/Labs/speech.c` |
| LAB08 | 8 | Первый радиоканал (HC-12, 433 МГц) | 🕒 модули в дороге |
| LAB09 | 9 | Второй радиоканал (nRF24L01+ по SPI4) | 🕒 |
| LAB10 | 10 | Сравнение каналов | 🕒 |

✅ **Арка «короткие провалы звука на приёме» ЗАКРЫТА** (tip `64b6fa3`, проверено на железе).
Механизм: воспроизведение — DMA NORMAL, пере-взвод в ISR каждую 1 мс; при `NVIC_PRIORITYGROUP_0`
(вытеснения нет) длинный приёмный ISR блокировал пере-взвод → щелчки. Лечение в два хода:
(1) разбор кадра вынесен из приёмного ISR в `Lab_Process` через SPSC-очередь (`60dfbe6`, LAB07);
(2) включено вытеснение, аудио-выход выше приёмного USART2 — сначала в LAB07 (`a96a72f`), затем
**обобщено на LAB04**; раскладка приоритетов вынесена в общий **`App/Common/preempt.{h,c}`**
(`Preempt_AudioOutHighest()`, зовётся из `Lab_Init` работы и после смены baud) (`64b6fa3`). Итог:
`probeA over` 133 → **0** (LAB07 ~10 мин; LAB04 проверен отдельно). Диагностика — счётчики
probe A/B/C (см. §11). Антиалиас-фильтр 16→8 тоже проверен на железе (`REPORT_aa_decim.md`).
**LAB00 намеренно без вытеснения** (петля без линии/консоли, конкурирующих ISR нет). Живой хвост
(Codec2/`loop_max`) — §8.

## 7. Ключевые факты для ближайших работ

- **Аудио (LAB00/LAB04):** BSP платы + PDM2PCM. PDM2PCM вынесен в **`ThirdParty/PDM2PCM`**
  (вне зоны CubeMX, иначе регенерация вычищает `Middlewares/`). Подключение библиотеки в
  `.cproject`: `-L ../ThirdParty/PDM2PCM/Lib` + `-l:libPDMFilter_CM4_GCC_wc32.a`. Единое
  тактирование — `App/Common/Src/bsp_audio_clock.c` (86 МГц, годится и для 8 кГц).
- **UART-провод (транспорт всех работ 02–07): `USART2`, `PA2`=TX, `PA3`=RX**, крест-накрест
  + **общая земля**. Приём — **кольцевой DMA1 Stream5 + событие простоя (IDLE)**
  (`HAL_UARTEx_ReceiveToIdle_DMA` / `HAL_UARTEx_RxEventCallback`). Общий код —
  `App/Common/uart_port.{h,c}`. APB1=24 МГц. **Рабочая скорость 460800**; на 921600 —
  ошибки кадра и потери. Скорость меняется командой `baud`.
  - ⚠️ **Ловушка:** после перепрошивки плата стартует со скоростью **из `.ioc`**, а не с той,
    что задавали командой `baud`. Выставлять `baud` заново после каждой прошивки.
  - ⚠️ **Ловушка:** смена `baud` при **активной передаче** подвешивает консоль — сначала
    `ptt off` и `tone off`, потом менять скорость.
  - ⚠️ **PA9/PA10 (USART1) физически НЕ работают** — см. §8 п.5. Не возвращаться туда.
- **Аудио 8/16 кГц + антиалиас (LAB07):** аппаратный тракт всегда 16 кГц (BSP не умеет менять
  частоту на лету — буферы/PDM жёстко под 16 кГц). 8 кГц = программная децимация 16→2:1.
  Децимация — **линейно-фазовым FIR-НЧ** (31 тап, Хэмминг, срез ~3.6 кГц, −60 дБ на 5 кГц),
  команда `decim fir|avg` (avg = старое усреднение, для сравнения). Тон-генератор синтезируется
  на **входной частоте 16 кГц** и проходит через фильтр. См. `docs/REPORT_aa_decim.md`.
- **Диагностика SWO/SWV (проверено):** Core Clock 96 МГц, Clock Prescaler 48, SWO 2000 кГц,
  **ITM порт 0**. В диалоге Configure Trace порты в строке Enable port нумеруются **справа
  налево — порт 0 самый правый**. Без отладчика вывод отбрасывается (ядро не блокируется).
  У LAB03–07 основная консоль — **USB CDC** (плата как COM-порт), SWO вспомогательно.
- **Железо / радиоканалы (впереди):**
  - **LAB08 — HC-12** (433 МГц, интерфейс UART): модули **в дороге, ещё не на руках**. Это
    первый радиоканал; встанет на тот же транспорт `uart_port` вместо провода.
  - **Управление направлением полудуплекса ГОТОВО** (RS-485 на MAX485E как отладочный стенд, провод
    идеальный): вывод `RS485_DE`=**PE7** (DE+/RE связаны), подъём до первого байта, опускание строго
    по TC, аварийное опускание + сторожевой таймер, защитные интервалы в мкс, выбор `phy ttl|rs485`
    командой, счётчики (команда `rs485`). Всё в транспорте `uart_port.c` (умолчание TTL — прежние
    работы не тронуты). **Эту же точку займёт драйвер HC-12** (та же последовательность разворота,
    его собственная задержка 4–80 мс в FU3). RO преобразователя → PA3 напрямую (PA3 FT по DS10314,
    делитель не нужен). Отчёты — `docs/REPORT_rs485_direction_recon.md` (этап 1),
    `docs/REPORT_rs485_direction2.md` (этап 2).
  - **LAB09 — nRF24L01+ по `SPI4`:** `PE12`=SCK, `PE13`=MISO, `PE14`=MOSI, плюс `CSN`=PE11,
    `CE`=PE10, `IRQ`=PE9 — всё на разъёме **P1, контакты 27–32**. Разведка — `docs/REPORT_spi_recon.md`.

## 8. Грабли (не наступать повторно) и следующие шаги

**Грабли:**
5. **PA9 удерживается внешней цепью платы** (остаток схемы VBUS-sensing), выход МК её не
   пересиливает (доказано: меандр `pintest` не доходил до 0 В). **USART1 на PA9/PA10 физически
   не работает — не возвращаться туда.** Именно поэтому весь обмен переехал на USART2 (PA2/PA3).
6. Каталог **`Middlewares/` вычищается генерацией CubeMX** — сторонние библиотеки держать
   ТОЛЬКО в `ThirdParty/` (как PDM2PCM; так же ляжет Codec2).
7. Периферию, которой распоряжается **BSP**, включать в `.ioc` со **СНЯТОЙ генерацией кода**
   (иначе CubeMX перезапишет то, что настраивает BSP).
8. **BSP не поддерживает смену частоты дискретизации на лету** — 8 кГц только программной
   децимацией, аппаратный тракт всегда 16 кГц.
9. **Тактовую 96 МГц НЕ поднимать:** при 100 МГц VCO=400 МГц и нет целого делителя для 48 МГц
   USB → пропадает USB-консоль (наш основной инструмент отладки). Неприемлемо.
10. Прежний опыт «тон 5 кГц отражался в 3 кГц» доказывал алиасинг **генератора тона** (NCO
    работал на 8 кГц), а НЕ фильтра децимации. Генератор перенесён на 16 кГц — теперь тот же
    тест честно проверяет именно антиалиас-фильтр.
11. **После прошивки `rate` возвращается к 16000, а скорость — к значению из `.ioc`** (не к
    заданным командами). Перед ЛЮБЫМ замером обе платы настроить заново (`rate 8000`, нужный
    `baud`) и проверить командой `voice`.
12. **LAB04 непригодна для длительных прослушиваний:** сырой PCM 16 кГц (~416 кбит/с с
    накладными ×1.3) на 460800 имеет запас всего ~1.1×, а на 921600 линия сыплется (замерено:
    `fe=15313` на приёмной плате). Чистого окна по скорости у неё нет — для долгих прогонов брать
    **LAB07** (сжатие).
13. **Строка `voice` печатает ЛОКАЛЬНУЮ настройку `codec`/`rate` платы**, а декодирование идёт по
    признаку в заголовке кадра — по строке приёмной платы НЕЛЬЗЯ судить, чем передавала вторая.
14. **После выноса разбора из ISR (`60dfbe6`) `recv` не растёт во время `ptt on`** (инкремент —
    внутри `voice_sink`, который на передаче не вызывается: очередь опустошается). Это норма.

**Следующие шаги (по решению владельца):**
- Арка провалов звука **закрыта** (см. §6, отчёты `REPORT_rx_gaps_recon.md`,
  `REPORT_isr_deadline_probe.md`, `REPORT_rx_isr_offload.md`, `REPORT_nvic_preemption.md`,
  `REPORT_preemption_other_labs.md`).
- **Хвост 1 — ЗАКРЫТ** (`64b6fa3`): вытеснение обобщено на LAB04, раскладка приоритетов в общем
  `App/Common/preempt.{h,c}` (`Preempt_AudioOutHighest()`). Вынос `voice_sink` в очередь для LAB04
  не потребовался — лечит именно вытеснение. **LAB00 намеренно оставлен без вытеснения** (петля
  без линии/консоли, конкурирующих тяжёлых ISR нет, на железе без провалов; плюс правило «не
  менять LAB00»). Появится НОВАЯ работа с аудио-выходом и тем же дефектом — звать
  `Preempt_AudioOutHighest()` из её `Lab_Init` (и после смены baud), guard в `preempt.c` дополнить.
- **Хвост 2 (ЖИВОЙ):** под Codec2 следить за `loop_max` (probeC, уже ~953 мкс) и `qover`: вокодер
  положит тяжёлый разбор кадра в главный цикл, и очередь `FQ_DEPTH=16` (80 мс) + `JB_PREFILL`
  20 мс станут ограничением. При росте — увеличить глубину/prefill (**prefill не уменьшать без
  спроса владельца**).
- **Codec2 Задача B:** перенос в `ThirdParty` (LGPL 2.1), **четвёртым кодеком в лесенку LAB07**
  (новую работу НЕ заводить), сразу замерить `load` на **3200**; не уложится в 96 МГц — уйти на
  **1300/700C** (это желаемый результат: −112…−117 дБм). Разведка — `docs/REPORT_codec2_recon.md`.

- **Перегрузка тракта на громкой речи — ЗАКРЫТА (headroom, проверено на железе).** Причина
  (`REPORT_adpcm_overload_recon.md`): высокий `mic_gain=24` в BSP без аттенюации → PCM у полной шкалы
  → хрип **ADPCM по крутизне** (не сам срез). Лечение (`REPORT_input_headroom.md`, LAB07-локально в
  `speech.c`, `20ffa7d`): команда **`headroom 0|3|6|9|12`** (аттенюация мик-PCM до кодека) +
  измеритель pre/post в `voice`. **Дефолт −6 дБ ПОДТВЕРЖДЁН** (громкая речь: хрип снят). Решающий
  опыт: громкий-6дБ и тихий-0дБ дают на кодек ту же амплитуду и звучат одинаково чисто → срез в
  PDM-фильтре на слух безвреден, лечится именно перегрузка ADPCM. **`mic_gain` в CubeMX не трогаем**
  (вариант 3 закрыт). **Оговорка:** индикатор перегрузки входа — `pre peak` (=32768 → срез в фильтре
  выше нашей точки); `post nearFS=0` — ложный зелёный (обнуляется масштабированием). Остаётся п.3
  приёмки (не проваливается ли тихая речь на 6 дБ) — владелец проверит отдельно; при нужде дефолт
  сменить одной строкой (`g_hrGain`/`g_hrDb`). Методичка: сравнение кодеков — на умеренной громкости
  (вокодер выигрывает в объёме канала, ADPCM — в узнаваемости).

**Незакрытых блокеров нет.** (Старый вопрос по PA10/PA9 закрыт: причина — внешняя цепь на PA9,
переезд на USART2; см. п.5.)

## 9. Переходные файлы для новой сессии (что читать при старте)

1. **`PROJECT_HANDOFF.md`** (этот файл) — общая карта и правила.
2. **`README.md`** — структура каталогов, лабы, настройка SWO.
3. **`docs/LABS.md`** — оглавление курса.
4. **`docs/TASK_*.md`** — тексты заданий Wagan's Team (текущее/будущие).
5. **`docs/REPORT_*.md`** — подробные отчёты по каждому шагу (что и почему сделано). Свежие/ключевые:
   арка провалов звука — `REPORT_rx_gaps_recon.md`, `REPORT_isr_deadline_probe.md`,
   `REPORT_rx_isr_offload.md`, `REPORT_nvic_preemption.md`, `REPORT_preemption_other_labs.md`;
   `REPORT_aa_decim.md` (антиалиас),
   `REPORT_codec2_recon.md` (разведка вокодера), `REPORT_spi_recon.md` (радио по SPI);
   `REPORT_lab03_framing.md`, `REPORT_lab04_voice_wire.md`, `REPORT_lab05_usb_console.md`,
   `REPORT_lab07_speech_compression.md`.
   Более ранние: `REPORT_lab_layout.md`, `REPORT_lab02_recon.md`, `REPORT_lab02_uart_link.md`,
   `REPORT_lab02_rx_dma.md`, `REPORT_i2s_clock_analysis.md`, `REPORT_swo_trace.md`,
   `REPORT_bsp_audio_integration.md`, `REPORT_pdm_relocate.md`, `REPORT_st_audio_example.md`,
   `REPORT_uart_rx_reference.md`.
6. Проектная память (загружается сама в новой сессии): `walkie-talkie-project`,
   `report-to-file-and-beep`, `build-before-commit`, `copyright-header`, `project-handoff-pointer`,
   `audio-gap-fixed-preemption`.

Для быстрого входа при смене чата достаточно `PROJECT_HANDOFF.md` + `docs/LABS.md` +
последние `docs/REPORT_*.md`, чтобы поставить следующую задачу `docs/TASK_*.md`.

## 10. Справочник консольных команд по работам

Источник — фактические таблицы `k_cmds[]` в коде. Консоль (модуль `App/Common/console.{c,h}`)
есть у **LAB03/LAB04/LAB05/LAB07** (по USB CDC, плата как COM-порт); во всех них доступна
встроенная команда **`help`** — печатает список (`console.c:266`, `builtin_help`). У **LAB00 и
LAB02 консоли нет** — взаимодействие только по SWO (`TRACE_LOG`).

- **LAB00** (`stand_check.c`): команд нет. Микрофон→колонки, по SWO — heartbeat «SWO <n>» раз в
  ~1 c и строка `status: blocks=… err=…` (`stand_check.c:124,127`).
- **LAB02** (`uart_link.c`): команд нет. По SWO — `TRACE_LOG` статистики (`stat: tx/rx/lost/crc/…`)
  и итог нагрузочного режима (`load done: …`); режимы PERIODIC/LOAD (`uart_link.c:118,137`).
- **LAB03** (`framing.c:194`): `send [n]`, `sendbyte`, `stat`, `reset`, `dump`, `regs`, `baud`,
  `mode legacy|slip` (схема кадрирования), `flood <n>` (пачка без пауз; **на `phy rs485` отказывает —
  полудуплекс**), `corrupt on|off [k]` (портить каждый k-й tx-байт), `proto` (статистика кадрирования),
  **`phy ttl|rs485`**.
- **LAB04** (`voice_wire.c:354`): `send`, `sendbyte`, `stat`, `reset`, `dump`, `regs`, `baud`,
  `voice`, `proto`, `ptt on|off`, `tone on|off`. ⚠️ **`tone` в LAB04 — жёстко 1 кГц, БЕЗ
  параметра** (в отличие от LAB07).
- **LAB05** (`usb_console.c:219`): `send [n]`, `sendbyte`, `stat`, `reset`, `dump`, `baud`,
  `regs`, `pintest` (дрыгать `PA[n]` ~1 кГц/5 с осциллографом, по умолч. PA2).
- **LAB07** (таблица `k_cmds[]` в `speech.c`): `send`, `sendbyte`, `stat`, `reset`, `dump`, `regs`,
  `baud`, `voice`, `proto`, `ptt on|off`, **`tone on [freq]|off`** (частота ЗАДАЁТСЯ, по умолч.
  1 кГц — напр. `tone on 5000`), `codec raw|ulaw|adpcm|codec2`, **`c2mode 3200|2400|1600|1300|700C`**
  (режим вокодера, старт 3200), **`headroom 0|3|6|9|12`** (аттенюация мик-PCM до кодека, дБ; запас
  по уровню против перегрузки на громкой речи; измеритель pre/post — в `voice`; дефолт −6 дБ
  провизорный), `rate 8000|16000` (при codec2 — только 8 кГц), `decim fir|avg`,
  `budget` (граница канала HC-12), `load` (загрузка ядра legacy-кодеком; для codec2 — см. `voice`),
  `c2load [mode]` (синтетический бенч Codec2, в тракт не подключён), **`phy ttl|rs485`** (физслой
  линии; rs485 включает секвенс направления DE=PE7), **`rs485`** (статистика разворота +
  `rs485 guard <pre> <post>` / `rs485 wd <margin>` — защитные интервалы/сторож в мкс).

## 11. Счётчики probe A/B/C (LAB07, печатает `voice`; обнуляет `reset`)

Диагностика арки провалов звука; оставлена для сравнения «до/после». `reset` обнуляет счётчики и
ставит базу отсчёта секунд для probe A.

- **`probeA out-rearm`** (`audio.c`): интервал между пере-взводами аудио-выхода (DMA NORMAL, ном.
  1 мс). `calls` — измеренных интервалов; `over(>1031us)` — превышений дедлайна; `max` — худший
  интервал, мкс; `first/last` — секунда первого/последнего превышения (от `reset`); `lastmin` —
  за последние ~60 с (`(capped)` — окно насыщено). **Порог 1031 мкс = 1 мс + один период слова
  I2S (31.25 мкс)**: у I2S нет глубокого FIFO, опоздание больше одного слова = слышимый провал.
- **`probeB rx-isr`** (`uart_port.c`): длительность приёмного ISR `HAL_UARTEx_RxEventCallback`.
  `max`/`avg` мкс, `long(>250us)` — число особо тяжёлых. Показывает запас до 1-мс дедлайна.
- **`probeC offload`** (`speech.c`): `qover` — переполнений очереди кадров (кадр принят, класть
  некуда; должно быть 0); `qdepth` — глубина очереди (16); `loop_max` — макс. период `Lab_Process`,
  мкс (добавленная задержка разбора; должна быть ≪ prefill 20 мс).

Ориентир «здорового» прогона (a96a72f, ulaw/8к/460800, ~10 мин): `probeA over=0`,
`probeB max≈205 мкс`, `probeC qover=0 loop_max≈950 мкс`.
