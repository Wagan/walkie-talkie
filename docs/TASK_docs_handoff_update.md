# TASK_docs_handoff_update — хендофф, справочник команд и правка отчёта по фильтру

Автор: Vagan Sarukhanov
Дата постановки: 17.08.2026
Место в репозитории: `docs/`

## Режим работы

**Только документация.** Код не трогать вообще — ни `App/`, ни `.ioc`, ни HAL/BSP. Сборка не
требуется. Всё содержание брать **из фактического кода и отчётов репозитория**, не по памяти:
любой список команд, порог или имя файла — подтверждать `grep`/чтением и указывать
файл:строка там, где это уместно.

## Основание: что показали замеры (для контекста правок)

Арка дефекта коротких прерываний звука закрыта. Итог по отчётам
`docs/REPORT_rx_gaps_recon.md`, `docs/REPORT_isr_deadline_probe.md`,
`docs/REPORT_rx_isr_offload.md`, `docs/REPORT_nvic_preemption.md`:

| этап | probeB rx-isr max | probeA over | худшее опоздание |
|---|---|---|---|
| до правок | 303 мкс | 133 за 195 с (0.68/с) | 1367 мкс |
| после выноса из ISR (60dfbe6) | 205 мкс | 385 за 539 с (0.71/с) | 1203 мкс |
| после вытеснения (a96a72f) | 234 мкс | **0** за 595 с | **1000 мкс** |

Прогон ~10 минут непрерывной передачи (ulaw, `rate 8000`, 460800): `over=0` при
`calls=595323`, на слух чисто во всех интервалах, где раньше шли серии; `qover=0`,
`under/lost/txdrop/fe/ore` нули, `loop_max=953 мкс`.

Отдельно проверен на железе антиалиас-фильтр децимации 16→8: при `decim avg` тон 5000 даёт
слышимый паразитный 3 кГц, при `decim fir` не слышен вовсе; тон 1000 одинаков в обоих
режимах; на 4100–4200 при `fir` слышна слабая остаточная свёртка (4100 → 3900 Гц) —
переходная полоса 4.0–4.6 кГц, ослабленная ~20 дБ.

## Что сделать

**1. Обновить `PROJECT_HANDOFF.md`** (по его фактическому пути в репозитории):

- статус: арка провалов звука закрыта, текущий tip `a96a72f`;
- коротко механизм и лечение: разбор кадра вынесен из приёмного ISR в `Lab_Process`
  (SPSC-очередь), группа приоритетов переведена в режим с вытеснением, аудио-выход выше
  приёмного USART2; **вытеснение ставится из `Lab_Init` в `speech.c`, то есть действует
  только в LAB07**;
- открытые хвосты в «следующие шаги»: (а) прочие работы остались без вытеснения; (б) под
  Codec2 контролировать `loop_max` (уже ~953 мкс) и `qover`, prefill без спроса не трогать.

**2. Добавить в хендофф раздел-справочник консольных команд по каждой работе.** Это закрывает
реальный пробел: сегодня список команд LAB04 пришлось узнавать через `help` на живой плате.
Для каждой работы (LAB00, LAB02, LAB03, LAB04, LAB05, LAB07) — таблица команд с однострочным
назначением и указанием, где она объявлена. Обязательно отметить различия, на которых легко
споткнуться: например, в LAB04 `tone` жёстко 1 кГц без параметра, а в LAB07 частота задаётся
(`tone on 5000`). Источник — только фактические таблицы команд в коде.

**3. Добавить в хендофф описание счётчиков probe A/B/C** — что печатает каждая строка, какие
пороги и откуда они взялись (порог A = 1 мс + один период слова I2S = 1031 мкс), и что
`reset` их обнуляет и ставит базу отсчёта секунд. Будущая сессия должна понимать эти строки
без раскопок.

**4. Пополнить раздел граблей фактами сегодняшнего дня:**

- после прошивки `rate` возвращается к 16000, а скорость — к значению из `.ioc`; перед любым
  замером обе платы настраивать заново и проверять командой `voice`;
- LAB04 непригодна для длительных прослушиваний: сырой PCM 16 кГц (~416 кбит/с с накладными)
  на 460800 имеет запас всего 1.1×, а на 921600 сыплется линия (замерено: `fe=15313` на
  приёмной плате). Чистого окна по скорости у неё нет;
- строка `voice` печатает **локальную** настройку `codec` платы, а декодирование идёт по
  признаку в заголовке кадра — по строке приёмной платы нельзя судить, чем передавала вторая;
- после выноса разбора из ISR `recv` не растёт во время `ptt on` (счётчик внутри `voice_sink`).

**5. Поправить `REPORT_aa_decim.md`:** там написано «на железе не проверялось». Заменить на
раздел с фактическими результатами проверки (см. «Основание» выше), включая объяснение
остаточной слышимости на 4100–4200 через переходную полосу.

## Результат

Изменённые документы + короткий `docs/REPORT_docs_handoff_update.md` со списком того, что
обновлено и откуда взяты списки команд. Коммит — за Wagan's Team. Затем **STOP**.
