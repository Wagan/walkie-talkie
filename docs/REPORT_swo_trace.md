# ОТЧЁТ: диагностический канал SWO/ITM — Часть 1 (TASK_swo_trace2)

Дата: 2026-08-10. Правила соблюдены: `.ioc`, HAL, CMSIS, `Drivers/BSP` не изменялись;
правки в `main.c` — только в блоках USER CODE; проект не собирался. Имена и поведение
CMSIS взяты из фактического `Drivers/CMSIS/Include/core_cm4.h`, процедура настройки — из
`docs/reference/um2609` (STM32CubeIDE user guide, Rev 18). **Часть 2 не начата** — она
только после подтверждения владельца (правило 6).

Во все наши исходники добавлена шапка с копирайтом **© 1991-2026 NCPR LLC (Flexlab)** и
автором **Wagan Sarukhanov** (App/*.c, App/*.h).

---

## A/B. Созданные файлы и точка вызова

Созданы:
- `App/Inc/trace_swo.h` — интерфейс: константа порта + функция вывода строки.
- `App/Src/trace_swo.c` — реализация через штатную CMSIS `ITM_SendChar()`.

Изменён `Core/Src/main.c` (только USER CODE):
- `USER CODE BEGIN Includes`: `#include "trace_swo.h"`.
- `USER CODE BEGIN PV`: `static uint32_t swoLastTick, swoSeq;`.
- `USER CODE BEGIN PFP`: прототип `static void SWO_EmitHeartbeat(void);`.
- `USER CODE BEGIN 3` (основной цикл): раз в ~1 c (`HAL_GetTick`) вызов `SWO_EmitHeartbeat()`.
- `USER CODE BEGIN 4`: `SWO_EmitHeartbeat()` — выводит `"SWO <n>\r\n"`, число форматируется
  вручную (без printf — Часть 1). **Из обработчиков прерываний не печатаем** (они ~1000/с).

Стимул-порт ITM вынесен константой `TRACE_SWO_ITM_PORT = 0` (`trace_swo.h`).

### Поведение вывода без отладчика / при выключенной трассировке (критично)
Используется штатная `ITM_SendChar()` из `core_cm4.h`. По её фактическому исходнику:
```c
if ((ITM->TCR & ITM_TCR_ITMENA_Msk) && (ITM->TER & 1)) {   // ITM включён И порт 0 включён
    while (ITM->PORT[0].u32 == 0) { __NOP(); }              // ждать готовности FIFO
    ITM->PORT[0].u8 = (uint8_t)ch;
}
return ch;
```
Биты `TCR.ITMENA` и `TER[0]` выставляет **отладчик** при включении SWV. Поэтому:
- **Без отладчика (или при выключенной трассировке) условие ложно** → функция не входит в
  цикл ожидания, не пишет в FIFO и **сразу возвращает управление, отбросив символ**. Ядро
  **не блокируется** — прошивка нормально работает автономно (важно для питания без отладчика).
- Busy-wait `while(PORT[0].u32==0)` достижим **только** когда отладчик уже включил ITM и порт 0
  (активная трассировка); тогда возможна кратковременная задержка, если приёмник SWO не
  успевает вычитывать. На автономный кит это не влияет.

Итог по пункту A2: **сразу возвращает управление и отбрасывает символ; не блокируется** (кроме
случая активной трассировки с непрочитанным FIFO).

---

## C. Последовательность настройки SWV в STM32CubeIDE 1.16.0

По UM2609 Rev 18 (§3.2.2.1 «SWV debug configuration» и §3.2.3.5 «SWV ITM Data Console»).

**Наш Core Clock = 96 МГц** — фактическая частота ядра из нашей конфигурации тактирования
(`.ioc`: `RCC.SYSCLKFreq_VALUE = RCC.HCLKFreq_Value = RCC.CortexFreq_Value = 96000000`;
переменная `SystemCoreClock` после `SystemClock_Config()` = 96000000). Именно 96 МГц вписывать
в поле [Core Clock].

Шаги (Debug Configurations → наша конфигурация STM32 Cortex-M):
1. **[Run] > [Debug Configurations…]**, выбрать конфигурацию проекта; вкладка **Debugger**.
2. Интерфейс: выбрать **[SWD]** (SWV работает только по SWD — §3.2.2.1).
3. Включить **[SWV]** (Serial Wire Viewer).
4. В [Clock Settings] задать **[Core Clock] = 96 (МГц)** — «must correspond to the value set by
   the application». Проверить можно, добавив `SystemCoreClock` в Expressions в отладке.
5. **[SWO clock]**: по умолчанию **вручную задавать не нужно** — IDE ставит максимально возможную
   частоту в зависимости от зонда и Core Clock. Если аппаратура не тянет высокую частоту SWO,
   включить **[Limit SWO clock]** и вписать предел в кГц. (Про поведение при неверном Core Clock
   UM2609 явной фразы не даёт — по построению неверный Core Clock приводит к неверному расчёту
   делителя SWO и, как следствие, к отсутствию/искажению данных; это логическое следствие, не
   дословная цитата документа.)
6. **[Apply]**, затем **[Debug]** — запустить сессию (зонд и плата подключены).
7. **[Window] > [Show View] > [SWV] > [SWV ITM Data Console]** — открыть консоль.
   (Для первого раза полезен и **[SWV Trace Log]** — видно, идут ли пакеты вообще.)
8. В открытой консоли/настройках трассировки включить **[ITM port 0]** (наш `TRACE_SWO_ITM_PORT`).
   Кнопка **[Configure Trace]** есть во всех SWV-видах; там же ITM Stimulus Ports (32 порта).
9. Нажать **красную кнопку [Start/Stop Trace]** на панели консоли — запустить приём.
10. Пустить программу (Resume) — строки `SWO <n>` пойдут во вкладку **Port 0**.

Чем запускается приём: именно кнопкой **[Start/Stop Trace]** (красная) в SWV-виде; без её нажатия
данные не отображаются, даже если сессия идёт.

Чего подтвердить нечем: UM2609 описывает процедуру обобщённо для STM32CubeIDE (Rev 18 документа),
не привязываясь к 1.16.0; расположение пунктов меню в 1.16.0 совпадает. **Отдельно:** документ
UM1842 не подтверждает работоспособность SWO именно на этой плате (вывод CN2 «Reserved», ST-LINK/V2
«supports only SWD») — поэтому факт прохождения трассы проверяется только опытом (это и есть цель
Части 1).

---

## D. Запасной вариант (без реализации): наблюдение без остановки ядра

Если SWO не заработает — основной запасной путь до появления USB CDC — **Live Expressions**
(STM32CubeIDE, UM2609 §3.1.6.1):
- Работает **поверх обычного SWD** — SWO/SWV не требуется. UM2609: «All three GDB servers support
  normal debug, live expressions and SWV». То есть на нашем ST-LINK/V2 (только SWD) сработает
  независимо от того, идёт ли трасса по SWO.
- **Не останавливает ядро:** значения выражений «sampled live during debug execution» — обновляются
  периодически, пока программа выполняется (без halt).
- Включается флажком **[Enable live expressions]** в Debug Configurations (по умолчанию включён).
  Вид: **[Window] > [Show View] > [Live Expressions]**; добавить туда глобальные переменные
  (напр. счётчик блоков, флаг ошибки из демо) и/или выражения вида `(i*4+offset)`.
- **Ограничения (UM2609):** (1) чем больше выражений — тем ниже частота опроса
  («An increased number of expressions being sampled results in a slower sample rate»);
  (2) показывает только **значения переменных/выражений**, а не поток текста/лог; (3) переменные
  должны быть **глобальными** (в памяти) и в области видимости; (4) нужна **активная сессия отладки**
  (зонд подключён) — это не автономная диагностика; (5) это опрос памяти по SWD, не событийный
  real-time поток.

Прочее (кратко, тоже без реализации): при отсутствии SWO и Live Expressions остаётся классический
приём — остановки на брейкпойнтах и просмотр переменных (но это уже с остановкой ядра), либо
позднее — двунаправленный канал по **USB Device CDC** (следующая лаба). Индикация светодиодами демо
(LED4/5/6) продолжает работать без отладчика как грубая диагностика.

---

## Публикация

Замечание о составе коммита (прозрачность): рабочее дерево содержит **непрокоммиченные правки
владельца** от перегенерации `.ioc` (не мои): в `Core/Src/main.c` изменён режим HSE в
`SystemClock_Config` — `RCC_HSE_BYPASS` → `RCC_HSE_ON` (переход на кварцевый резонатор, что и
объясняет, почему обе платы теперь стартуют); в `walkie-talkie.ioc` PH0/PH1 = `HSE-External-Oscillator`;
в `.cproject` переупорядочена запись `App` в `sourceEntries` (косметика, App остаётся папкой-источником).
Коммит фиксирует цельное состояние: базис владельца + мой код SWO (Часть 1) + копирайт-шапки.

- Отчёт: `docs/REPORT_swo_trace.md`.
- Хеш коммита: **`dc02e7757d6602a2a9a5f3de9ea38f50077a9992`** · Push: OK (`91a26ee..dc02e77 main -> main`, exit 0).
- (Хеш вписан вторым коммитом.)

*Часть 2 (printf через syscalls.c, уровни логирования, диагностика демо, вопрос по
walkie-talkie.launch, раздел README) — по этому же отчёту, отдельным разделом, только после
подтверждения владельца.*

---

# Часть 2 (E, F, G, H) — после подтверждения владельца

Часть 1 подтверждена опытом (строки heartbeat дошли до SWV ITM Data Console). Рабочие
значения, сообщённые владельцем: **Core Clock 96 МГц, Clock Prescaler 48, SWO Clock 2000 кГц,
ITM порт 0**. Правила соблюдены: syscalls.c/HAL/CMSIS/Drivers/BSP не изменялись; правки в
`main.c` отсутствуют (весь новый код — в `App/`); проект не собирался. Во все новые/изменённые
наши исходники добавлена копирайт-шапка **© 1991-2026 NCPR LLC (Flexlab)**, автор
**Wagan Sarukhanov**.

## E. Полноценный вывод (printf + уровни)

**E1. printf без правки syscalls.c.** По фактическому `Core/Src/syscalls.c`: `_write()` объявлен
`__attribute__((weak))` и в цикле вызывает `__io_putchar()`, которая там объявлена как
`extern int __io_putchar(int) __attribute__((weak))` **без сильного определения**. Штатный
способ перенаправить `printf` — дать свою **сильную** `__io_putchar()`. Она добавлена в
`App/Src/trace_swo.c`:
```c
int __io_putchar(int ch) { (void)ITM_SendChar((uint32_t)(uint8_t)ch); return ch; }
```
Теперь цепочка `printf → _write → __io_putchar → ITM_SendChar → SWO`. Сам `syscalls.c` не
тронут. Без отладчика `ITM_SendChar()` возвращает сразу — `printf` не блокирует ядро.

**E2. Интерфейс логирования с уровнями** — `App/Inc/trace_log.h` (header-only):
```c
#define TRACE_LOG(fmt, ...)  printf("[I] " fmt "\r\n", ##__VA_ARGS__)   /* обычное сообщение */
#define TRACE_ERR(fmt, ...)  printf("[E] " fmt "\r\n", ##__VA_ARGS__)   /* сообщение об ошибке */
```
Перевод строки добавляется сам; `##__VA_ARGS__` — расширение GCC (наш тулчейн). В шапке —
предупреждение: не вызывать из ISR (printf медленный).

## F. Диагностика демо (`App/Src/audio_loopback.c`, печать только вне ISR)

1. **При инициализации** — одно сообщение с фактическими параметрами (main-контекст):
   `TRACE_LOG("audio init OK: Fs=%u Hz, in ch=%u, pdmBuf=%u words, pcm slot=%u words x2, codec=OK mic=OK", ...)`
   → `Fs=16000, ch=1, pdmBuf=128, pcm slot=32`. Печатается после успешных `BSP_AUDIO_OUT_Init` и
   `BSP_AUDIO_IN_Init` (их результат = OK и есть суть сообщения).
2. **Статус в основном цикле, не чаще 1 Гц** — `AudioLoopback_Process()` темпируется по
   `HAL_GetTick()` и печатает `TRACE_LOG("status: blocks=%u err=%u", delta, loopbackError)`, где
   `delta = blockCount - предыдущий_снимок`. **Счётчик `blockCount` инкрементируется в ISR**
   (`Loopback_ConvertBlock`), **читается и печатается в основном цикле**. Разделяемые переменные
   объявлены `volatile` (`blockCount`, `loopbackError`); снимок 32-битного счётчика атомарен на M4.
3. **При ошибке** — сообщение с указанием функции BSP: каждое место ошибки выставляет
   `lbErrWho` (имя функции: `BSP_AUDIO_OUT_Init` / `BSP_AUDIO_IN_Init` / `BSP_AUDIO_OUT_Play` /
   `BSP_AUDIO_IN_Record` / `BSP_AUDIO_IN_Error_Callback` / `BSP_AUDIO_OUT_Error_CallBack`), а
   `Process()` один раз печатает `TRACE_ERR("audio error from %s", lbErrWho)`. Колбэки ошибок —
   в ISR, поэтому там только фиксируется имя и флаг, а печать вынесена в основной цикл.

Проверено: вызовов `TRACE_*`/`printf` внутри обработчиков прерываний нет (только в `Init` и
`Process`).

## G. walkie-talkie.launch — анализ пути журнала (решение за владельцем)

Текущий `.launch` уже содержит рабочие SWV-настройки (`enable_swv=true`, `swv_trace_hclk=96000000`,
`itmports=1:0:…` → включён ITM порт 0). Единственная **машинно-специфичная** строка —
`com.st.stm32cube.ide.mcu.debug.stlink.log_file` = `C:\Users\user\…\Debug\st-link_gdbserver_log.txt`.

Факт: **логирование выключено** (`com.st.stm32cube.ide.mcu.debug.stlink.enable_logging = false`),
поэтому этот путь **сейчас не используется** и на работу конфигурации не влияет.

Как сделать путь переносимым (в IDE: Debug Configurations → вкладка **Debugger** → поле
**[Log to file]**):
- **Проще всего — очистить путь** (пустое значение). Логирование выключено, так что пустой путь
  ничего не ломает; при желании включить лог позже путь задаётся заново.
- **Либо workspace-относительный** через переменную Eclipse, напр.
  `${workspace_loc:/walkie-talkie}/Debug/st-link_gdbserver_log.txt` — типовой механизм Eclipse-переменных.
  (UM2609 явно поддержку переменной для этого поля не описывает — стоит проверить в IDE.)

После устранения абсолютного пути `.launch` (уже с готовыми SWV-настройками) можно убрать из
`.gitignore` и закоммитить — тогда студент получит и рабочую конфигурацию SWV. **Решение о правке
и коммите оставлено владельцу**: сам файл я не менял и в коммит не добавлял (остаётся игнорируемым).

## H. README

В `README.md` добавлен раздел **«Диагностика по SWO (SWV ITM Data Console)»**: что такое SWO на
этой плате (PB3 через SB15; ST-LINK/V2 «только SWD», CN2 Reserved — подтверждено опытом),
почему нет VCP (у встроенного ST-LINK/V2 его нет), и пошаговая настройка приёма с **проверенными
значениями** (Core Clock 96 МГц, Prescaler 48, SWO Clock 2000 кГц = 96000/48, ITM порт 0).
Отдельно выделено предупреждение: в диалоге **Configure Trace**, строка **Enable port**,
порты нумеруются **справа налево — порт 0 самый правый** чекбокс.

## Публикация Части 2

- Созданы: `App/Inc/trace_log.h`. Изменены: `App/Src/trace_swo.c` (+`__io_putchar`),
  `App/Src/audio_loopback.c` (диагностика), `README.md`. `main.c` не менялся.
- Хеш коммита Части 2: `<PLACEHOLDER-2>` · Push: `<PLACEHOLDER-2>`
