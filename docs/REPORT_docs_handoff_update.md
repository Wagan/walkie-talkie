# Отчёт: обновление хендоффа, справочник команд, правка отчёта по фильтру

Только документация — код (`App/`), `.ioc`, HAL/BSP не трогались, сборка не требовалась. Все
списки команд взяты из фактических таблиц `k_cmds[]` в коде (файл:строка указаны в хендоффе).

## Что обновлено

1. **`PROJECT_HANDOFF.md`:**
   - шапка: дата 2026-08-19, tip `a96a72f`;
   - §6: отметка «арка провалов звука LAB07 ЗАКРЫТА», механизм и лечение (вынос разбора из ISR
     `60dfbe6` + вытеснение `a96a72f`, **действует только в LAB07** — ставится из `Lab_Init`
     `speech.c`);
   - §8: добавлены грабли 11–14 (после прошивки `rate`→16000 и скорость из `.ioc`; LAB04 не
     годится для долгих прослушиваний, `fe=15313` на 921600; `voice` печатает локальный codec;
     `recv` не растёт при `ptt on` после выноса из ISR); «следующие шаги» переписаны на два
     открытых хвоста (прочие работы на GROUP_0; под Codec2 следить за `loop_max`/`qover`);
   - **новый §10** — справочник консольных команд по каждой работе (LAB00/02 — консоли нет,
     только SWO; LAB03/04/05/07 — таблицы из кода, встроенный `help`), с отметкой различия
     `tone`: в LAB04 жёстко 1 кГц, в LAB07 задаётся (`tone on 5000`);
   - **новый §11** — счётчики probe A/B/C: что печатает каждая строка, пороги (A = 1 мс + период
     слова I2S = 1031 мкс) и что `reset` их обнуляет;
   - §9: обновлён список отчётов (четыре отчёта арки провалов) и память.

2. **`README.md`:** добавлен практический абзац — LAB04 на скорости впритык, для долгих прогонов
   брать LAB07; дефект провалов звука в LAB07 вылечен (кратко, со ссылкой на отчёт).

3. **`docs/REPORT_aa_decim.md`:** «на железе не проверялось» заменено на раздел с фактическими
   результатами (decim avg → паразитный 3 кГц; decim fir → тон 5000 не слышен; тон 1000 одинаков;
   остаток на 4100–4200 объяснён переходной полосой 4.0–4.6 кГц, ослабленной ~20 дБ).

## Отдельно: чистка упоминаний инструментов (по просьбе владельца)

В наших файлах репозитория обезличивающие упоминания заменены на **Wagan's Team**: заголовки
`docs/TASK_*.md` («ЗАДАНИЕ …»), короткие пометки об авторстве в заданиях и отчётах, ролевые слова
в `PROJECT_HANDOFF.md`, а также технические комментарии в `.gitignore` и `TASK_recon_env_and_repo.md`.
Код (`App/`), `README.md`, `LABS.md` таких упоминаний не содержали (совпадения были только на слове
«архитектура» = architecture — оставлено).

Осталось по решению владельца (см. чат): (а) трейлеры в ИСТОРИИ коммитов — чистятся только
перезаписью истории (сменят все хеши, на которые ссылаются отчёты); (б) политика трейлеров в
будущих коммитах; (в) путь `.claude/settings.local.json` в `.gitignore` — функциональный игнор
папки настроек инструмента, оставлен рабочим.
