# REPORT: LAB09 шаг 1 — проверка монтажа и живости DWM1000 (devid)

Автор: Wagan Sarukhanov · Дата: 2026-08-27 · Ветка `main`

Шаг 1 работы LAB09: конфигурация сборки, тонкий файл работы `App/Labs/uwb_link.c` и
консольные команды для аппаратного сброса модуля и чтения регистра идентификатора
`DEV_ID` (0x00) по SPI4. Радиообмена и речевого тракта на этом шаге нет, драйвер DecaWave
не переносился. Проверка на железе — за владельцем.

---

## 1. Фактическое состояние периферии (после генерации владельцем)

Сверено по `Core/Src/main.c`, `Core/Src/stm32f4xx_hal_msp.c`, `Core/Inc/main.h`.

**SPI4 — `MX_SPI4_Init()` присутствует** (`main.c`), параметры:
- Mode = Master, Direction = 2LINES (full-duplex), DataSize = 8 бит, NSS = SOFT (программный);
- CLKPolarity = LOW, CLKPhase = 1EDGE → **режим SPI 0 (CPOL=0, CPHA=0)**;
- FirstBit = MSB; BaudRatePrescaler = 2 (в `.ioc`; в LAB09 понижаем — см. §4);
- MspInit: `__HAL_RCC_SPI4_CLK_ENABLE()`, `__HAL_RCC_GPIOE_CLK_ENABLE()`,
  PE12/PE13/PE14 = AF5 (SCK/MISO/MOSI).

**GPIO порта E** (`MX_GPIO_Init`, тактирование GPIOE включено):
| Вывод | Ожидание (задача/PINOUT) | Факт в проекте | Вывод |
|---|---|---|---|
| PE12/13/14 | SPI4 SCK/MISO/MOSI AF5 | AF5 SPI4 | ✅ |
| PE11 (CSn) | push-pull выход, старт HIGH | `OUTPUT_PP`, `WritePin(PE11)=SET` | ✅ |
| PE10 (RSTn) | выход с открытым стоком | `OUTPUT_OD`, старт **LOW** (`WritePin(PE10)=RESET`) | ⚠️ тип ОК, стартовый уровень LOW (в PINOUT предлагался HIGH) |
| PE9 (IRQ) | вход | `GPIO_MODE_INPUT`, NOPULL | ⚠️ вход ОК (в PINOUT предлагался EXTI9) |

`RS485_DE_Pin` = PE7 (не конфликтует с PE10/PE11). Итог: список из раздела «что сделано
владельцем» задачи (SPI4 FD Master NSS Disable PE12/13/14, PE11 PP-выход, PE10 OD-выход,
PE9 вход) выполнен **полностью**. Оба ⚠️ — расхождения не с задачей, а с более амбициозным
*предложением* из `PINOUT_dwm1000_lab09.md` (раздел 2–3 там прямо помечен «предложение,
требует утверждения владельцем»). Для чтения `DEV_ID` оба безвредны (IRQ не используется;
начальный сброс модуля до `Lab_Init` не мешает, `Lab_Init` всё равно делает свою
последовательность сброса). Периферию не трогали, `.ioc` не трогали. STOP не потребовался.

## 2. Конфигурация сборки LAB09 и затронутые файлы

**Конфигурация LAB09** добавлена в `.cproject` как копия блока `<cconfiguration>` от LAB08
(процедура §5 хэндоффа). Перед правкой создана резервная копия
`.cproject.bak.before-lab09` (внесена в `.gitignore`). В скопированном блоке заменены:
`config…debug.800000000 → …900000000` (id конфигурации/folderInfo), `name="LAB08" →
"LAB09"`, `Defaults …|| LAB08 || → || LAB09 ||`, `buildPath …/LAB08 → …/LAB09`, builder id
`…8431748446` (уникальный, чтобы не дублировать общий с LAB07/LAB08), `LAB_ID=8 → LAB_ID=9`.
Внутренние id опций оставлены как у LAB08 (так же сосуществуют LAB07/LAB08 — валидно). CRLF
сохранён (проверено: CR=LF, lone-LF=0). Записи `scannerConfiguration`/`refreshScope` не
добавлялись — их нет и у LAB03…LAB08, они не нужны для сборки.

Прочие затронутые файлы:
- **создан** `App/Labs/uwb_link.c` (`#if LAB_ID == 9`, `Lab_Init`/`Lab_Process`);
- `.gitignore` — добавлена строка `.cproject.bak.before-lab09`;
- привязки к номеру работы — см. §3.

`Core/Src/main.c` не трогали.

## 3. Полный список привязок к номеру работы

Найдено грепом `LAB_ID` по всему дереву (кроме `docs/`). Реальные привязки, которые нужно
расширять при добавлении работы (остальные вхождения — собственные guard'ы работ/модулей):

| Файл | Было | Стало | Зачем |
|---|---|---|---|
| `App/Common/Inc/lab.h` | список поддержанных `LAB_ID` (`#elif` + текст `#error`) без 9 | добавлено `&& (LAB_ID != 9)` и `, LAB09 (9)` в текст | единая точка правды: иначе `#error` при LAB_ID=9 |
| `USB_DEVICE/App/usbd_cdc_if.c` | 2 места: `…|| (LAB_ID == 8)` (include консоли; приём `Console_RxFromISR` в `CDC_Receive_FS`) | `… || (LAB_ID == 9)` | LAB09 использует консоль по USB CDC; **обе правки внутри USER CODE** (BEGIN INCLUDE и BEGIN 6) |
| `App/Common/Src/console.c` | guard модуля `…|| (LAB_ID == 8)` | `… || (LAB_ID == 9)` | иначе `console.c` не компилируется для LAB09 → неопределённые `Console_*` |

**Проверенные, но НЕ требующие правки:**
- `preempt.c` (guard LAB04/07/08) — на шаге devid аудиовыхода нет, вытеснение не нужно;
  понадобится позже, на речевом этапе LAB09;
- `uart_port.c`, `frame.c`, `codec.c`, `audio.c`, `voice.c` — их guard'ы не включают 9,
  при LAB_ID=9 компилируются пустыми (LAB09 их не использует), символов не тянут;
- `trace_swo.c`, `bsp_audio_clock.c` — без guard (общие), нужны/безвредны.

## 4. Расчёт делителя SPI

- Тактирование (из `SystemClock_Config`): HSE 8 МГц → PLL (M=4, N=192, P=/4) → SYSCLK 96 МГц;
  `APB2CLKDivider = HCLK/1` → **PCLK2 (APB2) = 96 МГц**. SPI4 сидит на APB2.
- Потолок SPICLK в состоянии INIT (после сброса) — **3 МГц** (DW1000 UM Table 1).
- Выбран `SPI_BAUDRATEPRESCALER_64` → делитель 64 → **SCK = 96 МГц / 64 = 1.5 МГц** (≤ 3 МГц,
  с запасом на длинный жгут переходной платы). Прескейлер 32 дал бы ровно 3.0 МГц — на границе,
  не берём.
- Реализация: в `Lab_Init` переинициализируем **только экземпляр `hspi4`**
  (`hspi4.Init.BaudRatePrescaler = SPI_BAUDRATEPRESCALER_64; HAL_SPI_Init(&hspi4)`); `.ioc` и
  `MX_SPI4_Init` не трогаем (тот же приём, что `pintest` в LAB05 применяет к UART).
- Команда `spistat` печатает частоту, **вычисленную** из `HAL_RCC_GetPCLK2Freq()` и делителя из
  `BaudRatePrescaler`, а не константой.

## 5. Источник длительностей сброса и ожидания

Читался `mks-firmware/docs/reference/DW1000 User Manual DecaWave.pdf` (v2.17).
**Отдельных Datasheet-файлов (DW1000/DWM1000 Datasheet) в репозитории НЕТ** — есть только
User Manuals и Driver Library (проверены три папки `reference/` и `archive/`). Поэтому:
- **ожидание после снятия сброса** — из **UM Table 1** (§2.3.2): после снятия сброса цифровой
  LDO включается и модуль входит в INIT «примерно за 4 мс», затем INIT→IDLE за +5 мкс; в INIT
  уже допустим SPI ≤ 3 МГц. Берём `DW_RST_WAIT_MS = 5` мс (> 4 мс, с запасом);
- **длительность НИЗКОГО импульса RSTn** — конкретного числа в доступной документации **нет**
  (в UM RSTn описан как выход; физика вывода — в Datasheet, которого у нас нет). Взято
  консервативно `DW_RST_LOW_MS = 2` мс (заведомо с большим запасом), значение параметризовано
  `#define` и меняется одной строкой. Требование «открытый сток, высоким уровнем извне не
  подавать» соблюдено физически (PE10 = OD, запись 1 → высокоимпеданс) и обосновано
  `PINOUT_dwm1000_lab09.md` (со ссылкой на DWM1000 Datasheet v1.7 §3).

Прочие факты (подтверждены дословно, DW1000 UM): `DEV_ID = 0xDECA0130` (§7.2.2); порядок байт
на шине младшим вперёд → `30 01 CA DE` (§2.2.1.2, Figure 3); зарезервированные регистры →
`0xDEADDEAD` (§7.2); заголовок чтения одиночного регистра: бит7=0 (read), бит6=0 (без
суб-адреса), адрес в младших 6 битах, для DEV_ID = `0x00` (§2.2.1.2, Figure 2); MSB first; CSn
низкий всю транзакцию.

## 6. Команды работы

Консоль по USB CDC (как в LAB03–LAB08), встроенная `help`. Набор минимальный:
- `devid` — читает регистр `0x00`, печатает сырые 4 байта в порядке приёма, собранное
  32-битное значение и вердикт: совпадение с `0xDECA0130`, `0xDEADDEAD` (SPI жив, но адрес
  не тот), all-zeros (нет MISO), all-ones (MISO завис в 1), иначе — «мусор»;
- `dwreset` — повторить аппаратный сброс модуля по RSTn;
- `spistat` — фактическая частота SCK и режим CPOL/CPHA.

Вывод — только ASCII/английский (§4 п.7 хэндоффа). Копирайт в шапке файла — по §4 п.6.

## 7. Результат сборки и коммит

- **Сборка:** headless CubeIDE в отдельном workspace. `walkie-talkie/LAB09` и (для контроля
  общих файлов) `walkie-talkie/LAB08` — обе **0 errors, 0 warnings**. В логе видно: `uwb_link.c`
  компилируется с `-DLAB_ID=9` в LAB09 и уходит в пустой объект с `-DLAB_ID=8` в LAB08.
- **Коммит:** `e60da27` (реализация + базис владельца SPI4/GPIO); хеш вписан этим вторым коммитом.

## 8. Что осталось неясным / разошлось с ожиданиями

1. **Datasheet отсутствует.** Задача (§3) предписывала брать длительности «из DWM1000
   Datasheet Table 1 и DW1000 Datasheet §5.6», но этих файлов в `mks-firmware/docs` нет —
   только User Manual. Ожидание INIT взято из UM Table 1; минимум импульса RSTn в доступной
   документации не описан и задан консервативно (см. §5). **Прошу владельца** приложить
   оригинальный DW1000/DWM1000 Datasheet, если нужны паспортные минимумы — поправим `#define`.
2. **GPIO vs PINOUT-предложение.** PE9 сделан `INPUT` (в PINOUT предлагался `EXTI9`), PE10
   стартует в LOW (в PINOUT предлагался HIGH). По тексту задачи оба выполнены; для devid
   безвредны. Если владелец хотел EXTI9 / стартовый HIGH — это правка `.ioc` на его стороне
   (мы `.ioc` не трогаем); для речевого этапа LAB09 (IRQ по приёму) EXTI9 вероятно понадобится.
3. **Инерция от копии LAB08.** Блок LAB09 в `.cproject` унаследовал от LAB08 линковку
   `libcodec2_cm4.a` и `libPDMFilter…` — для devid не используются, безвредны (линковка
   прошла). При желании их можно убрать из конфигурации LAB09 отдельно.
4. **SCK 1.5 МГц** выбран консервативно (≤ 3 МГц с запасом на жгут); при надёжном монтаже на
   коротком жгуте можно поднять до 3 МГц (INIT) и до 20 МГц после выхода в IDLE — это предмет
   следующих шагов LAB09.

## Порядок проверки на железе (владелец)

Прошить LAB09 на кит с подключённым модулем, открыть консоль (COM-порт), выполнить `devid` —
ожидается `0xDECA0130`. Повторить на втором ките со вторым модулем (обе переходные платы).
`spistat` покажет фактическую SCK и режим; `dwreset` — повторный сброс при необходимости.
